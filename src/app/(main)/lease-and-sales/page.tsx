import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Lease And Sales",
};

const LeaseAndSalesPage = () => {
	return <div>Lease And Sales Page</div>;
};

export default LeaseAndSalesPage;
